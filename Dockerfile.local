# 📦 Node.js 베이스 이미지
FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# 패키지 복사 및 설치
COPY package*.json ./
RUN npm install

# 로컬 개발용 환경변수 파일 복사
COPY .env.local .env

# 전체 코드 복사
COPY . .

# 포트 열기
EXPOSE 3000

# 개발 서버 실행 (HMR)
CMD ["npm", "run", "dev"]